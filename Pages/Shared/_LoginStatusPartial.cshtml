@if (User.Identity.IsAuthenticated)
{
	<form method="post" class="form-check-inline" asp-page="/Account/login">
		welcome @User.Identity.Name
		<button type="submit" class="mb-2 btn btn-link"> Logout </button>
	</form>
}
else
{
	<a class="btn btn-link" asp-page="/Account/login">Login</a>
}
